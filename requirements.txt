# AIVA - AI Vision Assistant
# Dependencies for Vision + Voice + Client-Server Architecture

# Core
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# Face Recognition
face_recognition>=1.3.0

# AI Assistant (Cloud — optional, NOT in navigation loop)
google-generativeai>=0.3.0

# Object Detection (YOLOv8)
ultralytics>=8.0.0

# Depth Estimation (PyTorch + MiDaS)
torch>=2.0.0
torchvision>=0.15.0
timm

# Speech Recognition (Whisper — mobile client STT fallback)
openai-whisper>=20230918

# Microphone Capture (desktop mode only)
sounddevice>=0.4.6
scipy

# Text-to-Speech (desktop mode only)
pyttsx3

# WebSocket Server
websockets>=12.0

# OCR (EasyOCR — local, GPU-accelerated)
easyocr>=1.7.0

# Testing
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.0

# ONNX Runtime (MiDaS optimization — Task 3)
onnxruntime>=1.16.0

# JWT Authentication (Task 2)
PyJWT>=2.8.0
